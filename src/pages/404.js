import React from "react";
import Header from "../components/header"

export default () => (
  <div style={{ color: 'teal' }}>
    <Header headerText="404 Page Not Found!" />
    <p>We're sorry, but we cannot find that page.</p>
    <p>Here, let's get you back safely.</p>
  </div>
)